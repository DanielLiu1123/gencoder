---
sidebar_position: 10
---

# init

`init` command is used to initialize basic configuration for using gencoder.

```bash
gencoder init
```

## Configuration

`gencoder.yaml` file is used to define the configuration for generating code.
