{{#if (match columnType 'varchar.*|text') ~}}
    String
{{~ else if (match columnType 'bigint|int64') ~}}
    Long
{{~ else if (match columnType '.*int.*') ~}}
    Integer
{{~ else if (match columnType 'timestamp') ~}}
    java.util.Date
{{~ else if (match columnType 'enum.*') ~}}
    String
{{~^~}}
    String
{{~/if~}}