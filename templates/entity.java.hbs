/**
 * gencoder generated file: src/main/{{replaceAll properties.package '.' '/'}}/{{table.name}}.java
 */

package {{properties.package}};

/**
 * gencoder block start: table info
 * table: {{table.name}}
 * comment: {{table.comment}}
 * indexes:
{{#each table.indexes}}
 *   {{name}}: ({{#each columns}}{{name}}{{#unless @last}}, {{/unless}}{{/each}})
{{/each}}
 * gencoder block end: table info
 */
public record {{table.name}} (
    // gencoder block start: columns
{{#each table.columns}}
    /** {{comment}} */
    {{> 'type.partial.hbs' columnType=type}} {{name}}{{#unless @last}},{{/unless}}
{{/each}}
    // gencoder block end: columns
) {}
