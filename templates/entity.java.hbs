/**
 * gencoder generated file: src/main/{{package}}/{{name}}.java
 */

package {{package}};

/**
* {{name}} is a public record that represents a {{name}} entity.
*/
public record {{name}} (
// gencoder block start: columns
{{#each columns}}
    {{name}} {{name}}{{#unless @last}},{{/unless}}
{{/each}}
// gencoder block end: columns
) {}
