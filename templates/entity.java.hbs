/**
 * gencoder generated file: src/main/{{replaceAll properties.package '.' '/'}}/{{pascalCase table.name}}.java
 */

package {{properties.package}};

/**
 * gencoder block start: table info
 * <p> table: {{table.name}}
 * <p> comment: {{table.comment}}
 * <p> indexes:
{{#each table.indexes}}
 *   <p> {{name}}: ({{#each columns}}{{name}}{{#unless @last}}, {{/unless}}{{/each}})
{{/each}}
 * gencoder block end: table info
 */
public record {{pascalCase table.name}} (
    // gencoder block start: columns
{{#each table.columns}}
    /**
     * {{comment}}
     */
    {{> 'type.partial.hbs' columnType=type}} {{camelCase name}}{{#unless @last}},{{/unless}}
{{/each}}
    // gencoder block end: columns
) {}
